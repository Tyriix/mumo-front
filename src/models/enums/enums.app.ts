export enum Direction {
  Up = 'up',
  Right = 'right',
  Down = 'down',
  Left = 'left',
}

export enum HomepageSections {
  Home = 'home',
  About = 'about',
  Offer = 'offer',
  Contact = 'contact',
  Clients = 'clients',
}

export enum Role {
  Admin = 'admin',
  User = 'user',
}
